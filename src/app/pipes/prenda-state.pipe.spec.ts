import { PrendaStatePipe } from './prenda-state.pipe';

describe('PrendaStatePipe', () => {
  it('create an instance', () => {
    const pipe = new PrendaStatePipe();
    expect(pipe).toBeTruthy();
  });
});
